import*as THREE from"three";const mainCanv=document.getElementById("test"),scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,mainCanv.offsetHeight/mainCanv.offsetWidth,.1,100),renderer=new THREE.WebGLRenderer({alpha:!0});renderer.setSize(mainCanv.offsetWidth,mainCanv.offsetHeight),mainCanv.appendChild(renderer.domElement),renderer.setClearColor(0,0);const geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:65280}),cube=new THREE.Mesh(geometry,material);function animate(){requestAnimationFrame(animate),cube.rotation.x+=.01,cube.rotation.y+=.01,renderer.render(scene,camera)}scene.add(cube),camera.position.z=5,console.log(mainCanv.offsetWidth),animate();